# LogDoctor runs fully on the rule-based analyzer without any keys.
# These are OPTIONAL — only needed if you wire up the optional AI analyzer (lib/aiAnalyzer.ts).
# Without them, LogDoctor silently falls back to rule-based analysis only.

# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=...
